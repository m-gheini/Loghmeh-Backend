stages:
    - build
    
docker-build:
  image: docker:latest
  stage: build
  services:
    - docker:dind
  before_script:
    - docker login -u "leila1378" -p "fakheri1378" docker.io
  script:
    - docker build -t "index.docker.io/leila1378/myapp" .
    - docker push "index.docker.io/leila1378/myapp"
  only:
    - master

# stages:
#     - build

# build:
#     stage: build
#     script:
#         - echo "building..." 
#         - docker build -t myapp .
#         - docker run -d 8080:8080 myapp
#         - 